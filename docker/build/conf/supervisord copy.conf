[supervisord]
nodaemon=true

[program:backend]
command=bash -c 'cd /root/backend && yarn start'

[program:frontend]
command=bash -c 'cd /root/frontend && yarn start'

[program:mysqld]
command=mysqld

[program:apache2]
command=apachectl -D FOREGROUND