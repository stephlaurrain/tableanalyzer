import styles from "./column.module.scss";

function Column({ col_id,
  colKey,
  colName,
  colType,
  colNullable,
  colLength,
  colDefault,
  colCount,
  colDesc,
  colReference,
  colInfos,
  colModel,
  colComment,
  colMapped, id}) {

return (
  <div>COLUMNs</div>

);
}

export default Column;
